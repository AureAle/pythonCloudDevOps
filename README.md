# A repo to review some python functions

### To Run the Function run the next command inside each folder:

```bash
python main.py
```
### To test run the next command inside the tests folder:

```bash
pytest
```
 